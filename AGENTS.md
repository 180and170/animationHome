## Cursor Cloud specific instructions

This repository (`animationHome`) is currently an empty project scaffold — it contains only a `README.md`. There are no source files, dependencies, build systems, tests, or runnable applications as of the initial commit.

- **No dependencies** to install (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No lint/test/build/run commands** available yet.
- **No services** to start.

Once the project gains source code and a dependency manifest, update the VM environment setup script accordingly (e.g., `npm install`, `pip install -r requirements.txt`, etc.) and revise this section with relevant startup caveats and development workflow notes.
